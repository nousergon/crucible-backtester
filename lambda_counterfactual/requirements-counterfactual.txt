# Counterfactual rule fit Lambda — minimal deps. No langchain, no
# Anthropic — pure offline analysis (sklearn fits trees on captured
# (input → decision) pairs).
boto3>=1.34
pyyaml>=6.0
# replay.counterfactual fits DecisionTreeClassifier(max_depth=3) on
# rows extracted from decision_artifacts/ via DictVectorizer + emits
# tree structure via export_text.
scikit-learn~=1.4
numpy<2
