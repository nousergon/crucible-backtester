# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so

# Docker vendor staging (alpha-engine-lib copied here by
# infrastructure/deploy_health.sh before build, cleaned up on exit).
vendor/

# Virtual environment
.venv/
venv/

# Config (contains secrets)
config.yaml

# Local results
results/

# Temp / research.db local copy
*.db
*.db-wal
*.db-shm

# Design docs — never commit, keep local only
*-design-*.md
*-design.md

# macOS
.DS_Store

# IDE
.idea/
.vscode/
CLAUDE.md

# Gitleaks config (symlink to shared config)
.gitleaks.toml

.gitleaksbaseline.json

# mutmut mutation-testing working directories (config#1958 deliverable 5h)
mutants/
.mutmut-cache
