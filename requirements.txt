vectorbt~=0.26.1
numpy<2
plotly<6
pandas~=2.2
boto3~=1.34
pyyaml~=6.0
yfinance~=0.2.54
pyarrow~=14.0
ib_insync~=0.9.86
# alpha-engine-lib pulls in arcticdb + flow-doctor via extras; see
# preflight.py and the `from alpha_engine_lib.logging import setup_logging`
# call sites in backtest.py / evaluate.py / lambda_health/handler.py.
# Private repo — installs require ALPHA_ENGINE_LIB_TOKEN:
#   CI: .github/workflows/ci.yml sets a git URL insteadOf rewrite.
#   Spot: infrastructure/spot_backtest.sh does the same before pip install.
#   Lambda: lambda_health/Dockerfile installs from vendor/alpha-engine-lib
#          staged by infrastructure/deploy_health.sh from a sibling checkout.
alpha-engine-lib[arcticdb,flow_doctor] @ git+https://github.com/cipher813/alpha-engine-lib@v0.2.2
