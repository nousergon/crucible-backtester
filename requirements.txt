# Bumped 0.26.1 -> 0.28.5 for numpy 2 compat (config#2815) — NOT a drive-by
# bump, it's load-bearing: cvxpy>=1.9.2 below hard-requires numpy>=2.0.0
# (every cvxpy release from 1.8.0 on does), and 0.26.1 is fundamentally
# broken under numpy>=2 (`vectorbt/generic/enums.py` does `np.float_`,
# removed outright in numpy 2.0 -> AttributeError at `import vectorbt`,
# breaking vectorbt_bridge.py + every test importing it). The obvious next
# patch, 0.26.2, only band-aids ONE of several numpy-2 breaks (it wraps
# `numpy.lib.stride_tricks._broadcast_shape`, a removed private symbol, in a
# try/except) and still declares `numpy<2.0.0` in its own wheel metadata —
# pip correctly refuses to resolve it against numpy 2, and forcing the
# combo anyway still hits the `np.float_` AttributeError above at import
# time. 0.27.x+ removed all removed-numpy-alias usage; 0.27.3 specifically
# was ruled out here because it AttributeErrors against pandas~=2.2's
# `DatetimeIndex.freq` (unrelated pandas-side incompatibility, not numpy).
# 0.28.5 has neither problem: clean `import vectorbt` under numpy 2.4.6,
# functional Portfolio.from_signals()/.sharpe_ratio()/.sortino_ratio()
# smoke-tested directly, and the full backtester suite passes at 0.28.5
# (3103/3105; the other 2 failures are a pre-existing RAM-headroom guard
# unrelated to this change — see PR). No API-signature diff found between
# 0.26.x/0.27.x/0.28.x for the Portfolio methods vectorbt_bridge.py calls.
vectorbt~=0.28.5
# numpy<3 (no explicit upper bound near numba's actual ceiling) let a
# transitive dep drift: numba (pulled in by vectorbt~=0.28.5 above, not a
# direct dependency of this repo) hard-caps at numpy<=2.4 and raises
# ImportError at `_ensure_critical_deps()` otherwise. numpy 2.4.6 was the
# resolved version when 0.28.5 was validated (see comment block above);
# capping here reproduces that known-good resolution instead of floating
# onto whatever numpy 2.5+ PyPI resolves next. alpha-engine-config#2975,
# #2976.
#
# 2026-08-13: the ceiling moved. numba 0.67.0 declares `numpy<2.6,>=1.22`
# (numba 0.66 was the `<2.5` one this cap was written against), so the cap
# steps to <2.6 — and numba is DECLARED here rather than left transitive,
# because the ceiling this cap tracks belongs to numba and a cap whose
# reason lives in an undeclared package drifts silently the moment pip
# resolves an older numba. Verified in a clean 3.12 venv before the edit,
# not inferred from metadata: numpy 2.5.2 + numba 0.67.0 + vectorbt 0.28.5
# import cleanly AND `vbt.Portfolio.from_signals(...)` returns the correct
# total_return, i.e. the numba-compiled path actually executes.
numba>=0.67
numpy>=2.0,<2.6
# A VECTORBT ceiling, not a stale pin (alpha-engine-config-I8759). `vectorbt~=0.28.5`
# builds its plot templates at import time, and plotly 7.0.0 changed the
# validator those go through: measured on crucible-backtester-PR743, four test
# files fail at COLLECTION with `_set_compound_prop -> validate_coerce` inside
# `plotly/graph_objs/layout/_template.py`, before an assertion runs. Same shape
# as the numpy ceiling above: the break is at `import vectorbt`, not in our code.
#
# Raising it is verified the way that one is — a clean 3.12 venv where
# `import vectorbt` succeeds AND `vbt.Portfolio.from_signals(...)` returns the
# correct total_return, so the numba-compiled path actually executed — never
# inferred from metadata. `<7` rather than `<6` is that verification: Dependabot
# jumped `<6` straight to `<8` and 6.x had never been tried.
plotly<7
pandas~=2.2
boto3~=1.43
pyyaml~=6.0
yfinance~=1.7.0
pyarrow~=25.0
ib_insync~=0.9.86
# setuptools floor for PYSEC-2026-3447 (newly disclosed; CI's pip-audit gate
# started failing 2026-07-14 against the default 79.0.1 bundled by the
# GH Actions Python 3.11 image). Not a build-backend requirement here — direct
# pin exists solely to satisfy the security-audit gate.
setuptools>=84.0.0
# Replay harness migrated from langchain_anthropic.ChatAnthropic to
# krepis.llm.LLMClient's OpenRouter transport (alpha-engine-config-I2997,
# 2026-07-19). The pinned langchain-anthropic~=0.3 was removed in this
# change since no code imports it.
# Counterfactual rule fit (replay/counterfactual.py) trains a shallow
# decision tree (max_depth=3) on (agent_input → agent_decision) pairs
# extracted from the captured DecisionArtifact corpus. >85% match rate
# ⇒ the LLM is likely just running that tree at $X per call.
# `~=1.4` is PEP 440 shorthand for `>=1.4,==1.*` (NOT capped to the 1.4.x
# patch line) — pip's resolver is free to pick any 1.x release, and with
# numpy<3 above it lands on scikit-learn 1.9.0. Called out explicitly
# because sklearn 1.4.0/1.4.1.post1 pin `numpy<2.0` in their own wheel
# metadata (numpy-2 support landed at 1.4.2); a naive reading of `~=1.4`
# as "1.4.x only" would wrongly assume this pin needs bumping for
# numpy-2 compat. Verified via `pip install --dry-run`, config#2815.
scikit-learn~=1.9
# cvxpy added 2026-05-11 for analysis/portfolio_optimizer_backtest.py — replays
# the constrained MVO optimizer (alpha-engine PR #157) over historical synthetic
# predictions. Bumped to >=1.9.2,<1.10 for numpy 2 compat (config#2815) — same
# target range as crucible-executor's parallel migration, kept identical
# on purpose so the two repos don't drift apart. Saturday SF spot venv picks
# this up via `pip install -r requirements.txt`; local dev requires manual
# install.
cvxpy>=1.9.2,<1.10
# alpha-engine-lib pulls in arcticdb + flow-doctor via extras; see
# preflight.py and the `from alpha_engine_lib.logging import setup_logging`
# call sites in backtest.py / evaluate.py / lambda_health/handler.py.
# v0.7.1 adds the cost_telemetry row to transparency_inventory.yaml,
# read by analysis/cost_report.py for the rolling-baseline floor.
# v0.8.0 adds alpha_engine_lib.eval_artifacts (canonical eval-style
# artifact partition: {prefix}/{run_id}.json + latest.json sidecar with
# YYMMDDHHMM run_ids). Consumed here by recommendation_artifact +
# assembler + per-optimizer history writers.
# v0.10.0 adds DecisionArtifact schema_version=2 with optional
# model_metadata + full_prompt_context (deterministic executor decisions
# under L2308). Consumed here by replay/runner.py (skips deterministic
# captures with explicit marker rather than crashing on None.get(...)).
# v0.16.0 adds load_latest_eval_artifact + list_eval_artifacts for the
# canonical sidecar-resolution reader. Required by sibling alpha-engine
# executor's signal_reader.py (imported via test_simulate_via_deciders
# integration tests); cross-repo lockstep — bumping in sync with
# alpha-engine's lib pin to avoid the silent-downgrade pattern flagged
# in feedback_lib_pin_lockstep_cross_repo_composition.
# NOTE: flow-doctor extra is HYPHENATED — setuptools normalizes the lib's
# `flow_doctor` extra to `Provides-Extra: flow-doctor`; older pip doesn't
# normalize underscore->hyphen, so `[flow_doctor]` silently drops it (config#1178).
#
# The `contracts` extra pulls jsonschema, required by
# tests/test_predictions_consumer_contract.py to validate the predictions.json
# envelope the measurement-coverage reader consumes against the SHARED versioned
# Slot M schema in nousergon_lib.contracts — the M0 consumer half of config#1321
# (the predictor side already pins `contracts` for its producer conformance
# test). Without it the test's `pytest.importorskip` would not skip but ERROR at
# first validate() (module imports; jsonschema missing) — so the shared-schema
# gate would never actually run in backtester CI, defeating the cross-repo pin.
# Bumped 0.124.99 -> 0.124.115 (alpha-engine-config-I10226): the first
# published nousergon-lib carrying nousergon_lib.testing.debug_swallow_guard
# (nousergon-lib-PR398, branch nel-i10226-swallows) — the shared AST scanner
# tests/test_no_debug_only_swallows.py in this repo calls, lifted out of
# crucible-executor's original detector (alpha-engine-config-I10031,
# crucible-executor-PR547) on second adoption per policy-shared-code.
# 0.124.115 is the expected next auto-bumped patch tag (auto-version-bump.yml
# bumps on every merge to main that touches src/; 0.124.114 was the last
# published tag when this pin was written). MERGE ORDER: nousergon-lib-PR398
# merges and publishes FIRST — this repo's CI cannot resolve this pin, and
# tests/test_no_debug_only_swallows.py cannot import, until it does. If a
# different nousergon-lib PR merges first and the actual publish lands on a
# later patch, raise this pin to match.
nousergon-lib[arcticdb,flow-doctor,quant-stats,contracts] @ git+https://github.com/nousergon/nousergon-lib@v0.124.115
# ── Predictor-replay runtime deps (config#3031) ──────────────────────────────
# synthetic/predictor_backtest.py and analysis/scanner_predictor_research_free_
# backfill.py import crucible-predictor CODE in-process (sys.path), so THIS
# repo declares the libraries that code needs at runtime. The spot deps step
# previously co-installed crucible-predictor's whole requirements.txt into the
# same environment, which let predictor's numpy>=2.5.1 floor silently override
# the numpy cap above (numba/vectorbt hard ceiling) — every incompatible
# pin pair between the two files was a latent deps-step failure. The two
# requirements files are now NEVER co-resolved: predictor's checkout on the
# spot box is code-only, and these floors mirror predictor's own (all
# compatible with the numpy cap above). Guard: tests/test_spot_backtest_no_sibling_requirements.py.
scipy>=1.18.1
lightgbm>=4.7.0
catboost>=1.2.10
hmmlearn>=0.3.3
tqdm>=4.70.0
optuna>=4.9.0
psutil>=7.2.2

# krepis[openai] (MIT foundation) — direct dep for krepis.dates.resolve_trading_day
# (pipeline_common re-exports it) and, as of config#1300, krepis.console.console_url
# (emailer.py's analysis-page deep-link builder). Kept as a direct pin: PyPI dep
# (krepis is published) — NOT a git pin, so it is outside the nousergon-lib
# lockstep guard (which tracks only the lib git-tag pins). nousergon-lib >=
# v0.66.0 also pulls krepis transitively, but the direct pin documents the
# minimum. Bumped to >=0.12.0 (config#2291): krepis.email_sender.send_email
# gained dedup_key/dedup_window_min (S3-marker dedup) at v0.12.0 —
# emailer.send_digest_email now passes dedup_key on every call, which would
# TypeError against krepis <0.12.0 (dedup_key wasn't a recognized kwarg).
#
# [openai] extra (alpha-engine-config-I2997, 2026-07-19): replay/runner.py's
# ReplayConcordance target-model invocation (_invoke_target_with_schema)
# migrated off langchain_anthropic.ChatAnthropic to krepis.llm.LLMClient's
# OpenRouter transport (DeepSeek V4 Flash) — the fleet's off-direct-Anthropic
# migration. The extra pulls `openai` (used against OpenRouter's base_url).
# v0.12.2 is the earliest krepis tag carrying the OpenRouter unresolved/
# leaked-tool-call safety fix in krepis.llm; the >=0.12.0 floor here is one
# patch below that — bumped alongside this change (see the pin below).
#
# Bumped to >=0.18.0 (config#3006, gated on krepis-PR44 merging + publishing):
# LLMResult gains served_provider (the upstream backend OpenRouter actually
# routed to, e.g. "DeepInfra") — replay/runner.py reads it via getattr with
# a None default, so this bump is required for the field to ever be
# non-None, not for correctness against an older krepis (that path already
# degrades gracefully).
#
# Bumped to >=0.59.4 (alpha-engine-config-I7214): the FIRST published krepis
# carrying krepis.stage_coverage, which both Lambda handlers in this repo
# import to self-assert their declared output before returning. The prior
# >=0.55.0 floor happens to resolve to a release that has it, but does not
# REQUIRE one — and the handlers' ImportError guard is deliberately silent
# about the handler's own outcome, so an install that satisfied the old
# floor with an older krepis would leave the assertion emitting nothing,
# indistinguishable from an assertion that found nothing (config-I7334).
#
# Bumped to >=0.59.6 (alpha-engine-config-I7372): the FIRST published krepis
# whose `krepis.spot_bootstrap` renderer carries `--max-runtime-seconds`,
# `--extra-clone` and `--config-copy-if` — all three of which this repo's
# spot bootstrap now REQUIRES, and all three of which this repo is the reason
# the renderer has: it is the only fleet workload cloning three repos, and the
# only one staging a config conditionally. 0.59.4 has `spot_bootstrap` but not
# those flags, so a launcher resolving 0.59.4 would fail on unrecognized
# arguments AFTER a spot instance already exists and is billing. The floor is
# also what makes `tests/test_spot_bootstrap_no_inline_fork.py` meaningful:
# `scan_for_inline_bootstraps` ships in the same release.
# Bumped to >=0.59.7 (alpha-engine-config-I7393): the FIRST published krepis
# whose cost records carry the four contract columns the transparency
# inventory's `cost_telemetry` row asserts — schema_version, run_id,
# agent_id, model_name. This repo's concordance Lambda becomes a cost
# PRODUCER in this PR, so a resolve below 0.59.7 would emit rows missing
# them and keep that row the weekly substrate health check's single [FAIL].
# >=0.59.6 happens to resolve to 0.59.7 today; that is resolution luck, not
# a requirement.
#
# Raised to >=0.59.8 (alpha-engine-config-I7423): the first published krepis
# carrying `krepis.cost_sink.flush_default_sink()`, which lambda_concordance's
# handler now calls in a `finally`. Without it the emitted rows never leave the
# in-process buffer, so 0.59.7's four contract columns are correct and unwritten.
#
# Raised to >=0.59.21 (alpha-engine-config-I7878, krepis-PR172): the first
# published krepis carrying `krepis.router.resolve_model_spec` — the
# supported way to address ONE registered model through the router edge,
# which is what replay/runner.py's target-model call now uses instead of
# building a provider client itself. The same release also makes
# `served_model_for_deployment` resolve a BARE registry id, without which
# every pinned call raises the alias-masquerade error in krepis.llm on an
# otherwise successful response. Both halves are required: a resolve below
# 0.59.20 fails at import (no such attribute) rather than degrading, which
# is the intended shape — this floor is a hard dependency, not a preference.
# MERGE ORDER: krepis-PR172 merges and publishes FIRST; this repo's CI
# cannot install until it does.
#
# The number is derived, not guessed: krepis bumps the PATCH on every merge
# to main that touched src/ (auto-version-bump.yml), and the last published
# release is 0.59.20 (krepis-PR171, merged while PR172 was in review — which
# is exactly why this floor is 0.59.21 and not 0.59.20: a floor naming an
# already-published release would be silently satisfied by a krepis that does
# NOT carry resolve_model_spec). If a third krepis PR merges before PR172,
# raise this line to match the release PR172 actually produces.
# floor raised for alpha-engine-config-I8298 — 0.59.33 is the first krepis whose flow-doctor extra floors flow-doctor>=0.16.0; below it a TimeoutError from an unreachable api.telegram.org escapes TelegramNotifier.validate() on the IMPORT path (it killed alpha-engine-predictor-inference and degraded two postclose safety gates on 2026-08-24). flow-doctor reaches this repo via nousergon-lib[flow-doctor] -> krepis[flow-doctor].
# Raised to >=0.59.53 (alpha-engine-config-I10141, krepis-PR206): 0.59.53 is
# the first published krepis carrying the NYSE-calendar fix — pre-2025
# weekday dates (2016-01-01..2024-12-31) previously answered `True` for
# `is_trading_day`/`resolve_trading_day` unconditionally (holiday data only
# covered 2025+ and the guard only checked the future edge). Below this
# floor, `krepis.dates` silently misclassifies every pre-2025 US market
# holiday as a trading day; at/above it, a date before 2016-01-01 instead
# raises `TradingCalendarPrecedesCoverageError` rather than guessing.
krepis[openai]>=0.59.53  # floor raised for alpha-engine-config-I10141 (was >=0.59.39) — NYSE trading-calendar coverage/precedes-coverage fix, krepis-PR206
# Raised to >=0.59.23 (alpha-engine-config-I7925, krepis-PR175): the first
# published krepis carrying `krepis.aws remove-lambda-env`, the removal
# counterpart to merge-lambda-env — deploy_concordance.sh / deploy_
# counterfactual.sh call it (on the GHA runner, per the "Install krepis for
# deploy_*.sh alerts call" workflow step) to converge alpha-engine-replay-
# concordance / -counterfactual's environment against a denied-key set.
# MERGE ORDER: krepis-PR175 merges and publishes FIRST.
# Raised to >=0.59.24 (alpha-engine-config-I8030, krepis-PR176): 0.59.23's
# remove-lambda-env enumerated Lambda aliases unconditionally, including
# under --defer-publish (which both deploy_*.sh scripts pass), and the
# github-actions-lambda-deploy role holds no lambda:ListAliases — a partial
# deploy (image pushed, alias never moved). 0.59.24 skips the enumeration
# under --defer-publish.
# Bumped to >=0.24.1 on main (2026-07-29). krepis 0.23.0 added callsite_id
# as a REQUIRED keyword-only argument to LLMClient.__init__ (a breaking change
# with no default) — replay/runner.py's _invoke_target_with_schema passes
# callsite_id="replay-concordance" (the row this callsite owns in
# alpha-engine-config's LLM_CALLSITE_REGISTRY.yaml). krepis <0.23.0 raises
# TypeError on that keyword, so the floor must match the actual parameter
# the code passes.
#
# Bumped to >=0.57.0 (alpha-engine-config-I7179, krepis-PR140): LLMClient
# now resolves a default cost_sink from KREPIS_COST_SINK_BUCKET/
# KREPIS_COST_SINK_PREFIX when the caller does not pass one explicitly —
# replay/runner.py's _invoke_target_model never has, so every DeepSeek call
# this module makes was landing on no cost record. The env wiring lives in
# infrastructure/deploy_concordance.sh (merge-lambda-env), not here; this
# floor just ensures the installed krepis is new enough to read it.

# alpha-engine-config#7199 — pytest is a RUNTIME dependency of this repo, not
# only a dev one. The backtester-to-executor replay parity artifact
# (`backtest/{run_date}/parity_report.json`, the proof that the simulation's
# fills resemble what the live executor would actually do) is produced by
# `python -m pytest tests/test_parity_replay.py -m parity`, invoked from
# infrastructure/spot_parity_replay.sh, spot_parity.sh and spot_backtest.sh on
# the spot instance.
#
# It used to arrive transitively: `spot_common_install_deps` co-installed the
# PREDICTOR repo's requirements, which carry `pytest>=9.1.1`. PR #550
# (2026-07-20, config#3031) stopped doing that — correctly, it was co-resolving
# a conflicting numpy floor — and took pytest with it, unnoticed. From the
# 2026-07-24 run onward the parity stage printed `No module named pytest`,
# exited in one second, and the SF routed to the SUCCESS path: 4+ weeks with no
# real parity_report.json (last: 2026-07-18, for run_date 2026-07-17) while the
# sibling report.md in the same S3 prefix kept updating weekly and masked it.
#
# SOTA note: the correct end state is a real CLI entry point
# (`python -m replay.parity_report`) so that a TEST RUNNER is not a production
# dependency. That is a larger change than restoring the artifact — tracked as
# alpha-engine-config-I7212, which REMOVES this pin. This restores the producer
# now, four weeks late.
pytest>=9.1.1
