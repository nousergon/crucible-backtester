# Concordance Lambda — minimal deps. Mirrors lambda_health/requirements-health.txt
# in spirit (lightweight, no LightGBM / vectorbt / ArcticDB).
boto3>=1.34
pyyaml>=6.0.3
# replay.runner invokes target models via krepis.llm.LLMClient's OpenRouter
# transport (alpha-engine-config-I2997, 2026-07-19 — migrated off
# langchain_anthropic.ChatAnthropic) against the canonical agent_schemas
# Pydantic contract (lib v0.4.0 — pulled in above via the nousergon-lib
# install). krepis[openai] (which provides the `openai` package this needs)
# is installed in the Dockerfile alongside nousergon-lib, not here.
pydantic>=2.13.4
